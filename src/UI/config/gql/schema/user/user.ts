const User = `
    type User { 
        uuid: String, 
        email: String,
        name: String,
        surname: String,
        identityId: String,
        createdAt: String
    }
`;

export default User;